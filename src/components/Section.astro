---
interface Props {
  id: string;
  title: string;
}
const { id, title } = Astro.props as Props;
---

<section 
  id={id}
  title={title}
  class="w-full min-h-[500px] flex flex-col items-center justify-center !py-24" 
>
  <slot />
</section>
